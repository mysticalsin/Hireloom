# Personal data (user fills these)
data/applications.md
data/pipeline.md
data/scan-history.tsv
reports/*.md
!reports/.gitkeep
output/*
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/*.tsv
!batch/tracker-additions/.gitkeep
# Batch summaries + cover-letter prompts — generated per-user, contain PII
batch/batch-*-summary.md
batch/cover-letter-prompt.md
batch/cover-letters/
jds/*
!jds/.gitkeep

# User config and customization (never auto-updated)
config/profile.yml
portals.yml
modes/_profile.md
.update-dismissed
.update-lock

# Generated
.resolved-prompt-*
node_modules/

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard

# ── Environment secrets — NEVER commit these ────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
secrets.json

# User-supplied resume + generated personal artifacts (contain PII)
cv.md
article-digest.md
data/follow-ups.md
# Personal product/strategy notes — keep local, not in public repo
docs/product-strategy.md
docs/personal-*.md
# story-bank.md accumulates personal STAR+R stories — never commit
# (the empty template lives at templates/story-bank.template.md and
# is copied into interview-prep/story-bank.md on first launch)
interview-prep/story-bank.md
interview-prep/*.md
!interview-prep/.gitkeep

# Backups + transient state
backups/
.dashboard.log
.cache/
tmp/
.tmp/

# Build / release artifacts (we don't track binaries; CI publishes them)
dist/
build/
*.exe
career-ops.tar.gz

# OAuth tokens & local caches with PII
data/gmail-tokens.json
data/gmail-cache.json
data/autopilot-log.json
data/autopilot-manual.json
data/dash-debug.json
data/high-paying-queue.json
data/high-paying-queue.md
data/apply-queue.md
data/pipeline.md.bak
# Structured error log (rotates to errors.log.1; both contain stack traces
# with absolute paths — never commit)
data/errors.log
data/errors.log.1

# Worktrees + agent scratch
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/audit.log
.conductor/
dashboard-web/.conductor/
# Session memory + planning scratch (per-user, contains paths + branch state)
.planning/
PROGRESS.md
CODEMAP.md
docs/solutions/

# Test scratch
tests/__fixtures__/tmp/
.pr-body.md

# Misplaced cross-drive paths (Windows: when a tool drops "E:/..."
# inside this repo, the colon gets encoded to U+F03A by Win32; treat
# any literal "E:/" or its encoded form as junk that must never ship.
# The literal-codepoint rule lives at the bottom of this file because
# git is byte-strict and the U+F03A char is invisible in most editors).
E:/

# ── Local experimental scripts (per-user, may contain PII) ──────────────────
# These belong to individual user sessions / forks. The canonical features
# they prototype eventually land in tracked scripts (auto-apply.mjs, scan.mjs,
# etc.). Until then, keep them local.
apply-one.mjs
assemble-package.mjs
auto-pipeline.mjs
jobseeker.mjs
keep-alive.bat
llm-api.mjs
obsidian-sync.mjs
scan-portals.mjs
tailor-resume.mjs
test-llm-pipeline.mjs
validate-readiness.mjs
batch/.js-prompt-*.md
batch/jobseeker.pid
batch/tracker-additions/merged/
batch/tmp/
# (literal E + U+F03A, the Win32 encoding of the colon when E:/ leaks into a repo)
E/

# Obsidian vault (this project doubles as an Obsidian vault; per-machine settings)
.obsidian/

# --- Migration / git-readiness hardening (2026-06-10) ---
# Persistent browser profiles — contain live login/session cookies, NEVER commit
.apply-profile/
.indeed-profile/
.apply-session/
.agents/
.qwen/
# Large interview recordings (1–5GB each)
interview-prep/recordings/
# Personal analysis spreadsheets
*.xlsx

# ── Personal memory layer (per-user instances — machinery ships in CLAUDE.md, content never does)
CLAUDE.local.md
WORKING.md
TOOLKIT.md
career-log.md
BUILD-CHANGELOG.md
Hireloom - Change Log*
archive/
portals.yml.bak.*

# Interview prep is personal — ship only the generic tools
interview-prep/*
!interview-prep/transcribe.py
!interview-prep/sample-frames.py
!interview-prep/_strip-pdf.mjs
!interview-prep/_strip-serve.mjs

# More user-layer data
data/hard-no.md
data/verify-urls.txt
reports/*/

# Personal content corpora + one-off build-run scripts (candidate-specific CV/cover
# content baked in — genericize before upstreaming, never commit as-is)
batch/wave*.mjs
batch/packages-data.mjs
batch/packages.mjs
batch/ports-data.mjs
batch/ports.mjs
batch/_experience-block.html
batch/build-cvs.mjs
batch/build-technical-variants.mjs
batch/regen74.mjs
batch/sample-accuratehd.mjs
batch/indeed-build.mjs
batch/indeed-covers.mjs
batch/build-pool-packages.mjs
batch/build-indeed-packages.mjs
batch/tailor-pool.mjs
